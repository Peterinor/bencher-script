{
    "demo": {
        "index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"]
        },
        "post-index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"],
            "postfile": "urls.json"
        }
    }
}