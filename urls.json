{
    "demo": {
        "index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"]
        },
        "post-index": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        },
        "offer": {
            "url": "{{server}}/offer-res.json",
            "headers": ["{{userAgent}}"],
            "timeout": 10
        }
    },
    "demo-2": {
        "test": {
            "url": "{{server}}/",
            "timeout": 1
        },
        "post-test": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        }
    }
}