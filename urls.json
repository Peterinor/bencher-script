{
    "demo": {
        "index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"]
        },
        "post-index": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        }
    }
}