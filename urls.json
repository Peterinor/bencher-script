{
    "demo": {
        "index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"]
        },
        "post-index": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        },
        "offer": {
            "url": "{{server}}/promotions.json",
            "headers": ["{{userAgent}}"],
            "timeout": 1
        }
    },
    "demo-2": {
        "test": {
            "url": "{{server}}/",
            "timeout": 1
        },
        "post-test": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        }
    }
}