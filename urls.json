{
    "demo": {
        "index": {
            "url": "{{server}}/",
            "headers": ["{{userAgent}}"]
        },
        "post-index": {
            "url": "{{server}}/",
            "postfile": "urls.json"
        },
        "offer": {
            "url": "{{server}}/offer-res.json",
            "headers": ["{{userAgent}}"]
        }
    }
}